import React from 'react';

const StreakDisplay = ({ streak }) => {
    // TODO: Visualize streak count (maybe with fire icon)
    // WHY: Gamification element to motivate user

    return (
        <div className="streak-display">
            {/* TODO: Show streak number */}
        </div>
    );
};

export default StreakDisplay;

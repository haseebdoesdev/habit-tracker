import React from 'react';

const HabitCard = ({ habit, onLog }) => {
    // TODO: Display habit details (title, streak)
    // WHY: Show individual habit info

    // TODO: Add completion button/checkbox
    // WHY: Allow user to mark habit as done for today

    return (
        <div className="habit-card">
            {/* TODO: Render habit info */}
        </div>
    );
};

export default HabitCard;

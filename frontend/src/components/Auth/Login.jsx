import React, { useState } from 'react';
// TODO: Import useAuth hook
// TODO: Import useNavigate

const Login = () => {
    // TODO: Define state for email and password
    // WHY: Controlled components need state to track user input

    // TODO: Define handleLogin function
    // WHY: Process form submission

    return (
        <div className="login-container">
            {/* TODO: Create form with email and password inputs */}
            {/* WHY: User needs interface to enter credentials */}
        </div>
    );
};

export default Login;

import React, { useState } from 'react';

const CreateHabit = () => {
    // TODO: Define form state (title, frequency, etc.)
    // WHY: Capture habit details

    // TODO: Handle form submission
    // WHY: Send new habit data to API

    return (
        <div className="create-habit">
            {/* TODO: Render form */}
        </div>
    );
};

export default CreateHabit;
